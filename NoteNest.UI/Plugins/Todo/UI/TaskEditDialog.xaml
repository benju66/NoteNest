<Window x:Class="NoteNest.UI.Plugins.Todo.UI.TaskEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Task"
        Height="250" Width="420"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Title" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TitleTextBox" Padding="8"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="DescriptionTextBox" 
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    Height="80"
                    Padding="8"
                    VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Save" Width="80" Click="SaveButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>