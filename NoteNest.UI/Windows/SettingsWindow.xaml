<Window x:Class="NoteNest.UI.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:NoteNest.UI.Converters"
        Title="Settings" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackgroundBrush}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Settings" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Settings Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Theme Section -->
                <TextBlock Text="Appearance" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <Border Background="{DynamicResource AppSurfaceBrush}" 
                       BorderBrush="{DynamicResource AppBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                       Padding="15"
                       Margin="0,0,0,20">
                            <StackPanel>
                        <TextBlock Text="Theme" Margin="0,0,0,8"/>
                        <ComboBox x:Name="ThemeComboBox" 
                                 SelectionChanged="ThemeComboBox_SelectionChanged"
                                 SelectedIndex="0">
                            <ComboBoxItem Content="Light" Tag="Light"/>
                            <ComboBoxItem Content="Dark" Tag="Dark"/>
                            <ComboBoxItem Content="Solarized Light" Tag="SolarizedLight"/>
                            <ComboBoxItem Content="Solarized Dark" Tag="SolarizedDark"/>
                        </ComboBox>
                            </StackPanel>
                        </Border>
                        
                <!-- General Section -->
                <TextBlock Text="General" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <Border Background="{DynamicResource AppSurfaceBrush}" 
                       BorderBrush="{DynamicResource AppBorderBrush}"
                       BorderThickness="1"
                                CornerRadius="4" 
                       Padding="15"
                       Margin="0,0,0,20">
                            <StackPanel>
                        <CheckBox x:Name="AutoSaveCheckBox" Content="Auto-save notes" Margin="0,0,0,10"/>
                        <CheckBox x:Name="SpellCheckCheckBox" Content="Enable spell check" Margin="0,0,0,10" IsChecked="True"/>
                        <TextBlock Text="Note: More settings will be added in future updates" 
                                  Opacity="0.6" FontSize="11" FontStyle="Italic"/>
                            </StackPanel>
                        </Border>
                </StackPanel>
            </ScrollViewer>
        
        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Close" Width="80" Click="CloseButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
