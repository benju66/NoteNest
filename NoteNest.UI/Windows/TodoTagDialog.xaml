<Window x:Class="NoteNest.UI.Windows.TodoTagDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Tags" 
        Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppTextPrimaryBrush}"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- Import Lucide icons -->
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/LucideIcons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Todo info -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Manage Tags" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock x:Name="TodoTextBlock" 
                       Text="Todo: " 
                       FontSize="12" 
                       Foreground="{DynamicResource AppTextSecondaryBrush}"/>
        </StackPanel>
        
        <!-- Tags display -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Add or remove manual tags below. Auto-inherited tags cannot be removed here." 
                       FontSize="11" 
                       Foreground="{DynamicResource AppTextSecondaryBrush}"
                       Margin="0,0,0,8"
                       TextWrapping="Wrap"/>
            
            <!-- Auto-inherited tags (read-only) -->
            <TextBlock Text="Auto-Inherited Tags:" 
                       FontWeight="SemiBold" 
                       Margin="0,0,0,4"
                       Visibility="{Binding ElementName=AutoTagsListBox, Path=HasItems, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <ListBox x:Name="AutoTagsListBox"
                     MaxHeight="60"
                     Background="{DynamicResource AppSurfaceBrush}"
                     BorderBrush="{DynamicResource AppBorderBrush}"
                     Foreground="{DynamicResource AppTextSecondaryBrush}"
                     Margin="0,0,0,8"
                     IsEnabled="False">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl Template="{StaticResource LucideZap}"
                                            Width="12" Height="12" 
                                            Foreground="#10B981"
                                            Margin="0,0,6,0"
                                            VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center" FontStyle="Italic"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <!-- Manual tags (editable) -->
            <TextBlock Text="Manual Tags:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ListBox x:Name="ManualTagsListBox"
                     Height="120"
                     Background="{DynamicResource AppSurfaceBrush}"
                     BorderBrush="{DynamicResource AppBorderBrush}"
                     Foreground="{DynamicResource AppTextPrimaryBrush}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl Template="{StaticResource LucideTag}"
                                            Width="14" Height="14" 
                                            Foreground="{DynamicResource AppAccentBrush}"
                                            Margin="0,0,8,0"
                                            VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        
        <!-- Add new tag -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12" VerticalAlignment="Top">
            <TextBox x:Name="NewTagTextBox" 
                     Width="300" 
                     Height="28"
                     VerticalContentAlignment="Center"
                     Background="{DynamicResource AppSurfaceBrush}"
                     Foreground="{DynamicResource AppTextPrimaryBrush}"
                     BorderBrush="{DynamicResource AppBorderBrush}"
                     KeyDown="NewTagTextBox_KeyDown"/>
            <Button Content="Add Tag" 
                    Width="80" 
                    Height="28"
                    Margin="8,0,0,0"
                    Background="{DynamicResource AppAccentBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Click="AddTag_Click"/>
            <Button Content="Remove" 
                    Width="80" 
                    Height="28"
                    Margin="8,0,0,0"
                    Background="{DynamicResource AppSurfaceBrush}"
                    Foreground="{DynamicResource AppTextPrimaryBrush}"
                    BorderBrush="{DynamicResource AppBorderBrush}"
                    BorderThickness="1"
                    Click="RemoveTag_Click"
                    x:Name="RemoveTagButton"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" 
                    Width="80" 
                    Height="32"
                    Background="{DynamicResource AppSurfaceBrush}"
                    Foreground="{DynamicResource AppTextPrimaryBrush}"
                    BorderBrush="{DynamicResource AppBorderBrush}"
                    BorderThickness="1"
                    Click="Close_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>

