<Window x:Class="NoteNest.UI.Windows.MemoryDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Memory Dashboard - Debug" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        MinHeight="400" MinWidth="700">

    <Window.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundAltMediumBrush}"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="6"/>
        </Style>
        
        <Style x:Key="StatValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Opacity" Value="0.7"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,12,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Stats Cards -->
            <RowDefinition Height="*"/>    <!-- Main Content -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <ui:SymbolIcon Symbol="Setting" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="Memory Dashboard" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <Border Background="{DynamicResource SystemControlBackgroundAccentBrush}" 
                    CornerRadius="10" Padding="8,2" Margin="12,0,0,0">
                <TextBlock x:Name="StatusText" Text="Tracking..." Foreground="White" FontSize="11"/>
            </Border>
            
            <!-- Refresh and Clear buttons -->
            <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" 
                        Margin="0,0,8,0" Padding="12,4"/>
                <Button x:Name="ClearButton" Content="Clear Data" Click="ClearButton_Click" 
                        Padding="12,4"/>
            </StackPanel>
        </StackPanel>

        <!-- Stats Cards -->
        <UniformGrid Grid.Row="1" Rows="1" Columns="4" Margin="0,0,0,16">
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock x:Name="TotalMemoryText" Text="127 MB" Style="{StaticResource StatValueStyle}"/>
                    <TextBlock Text="Total Memory" Style="{StaticResource StatLabelStyle}"/>
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock x:Name="DeltaMemoryText" Text="+45 MB" Style="{StaticResource StatValueStyle}"/>
                    <TextBlock Text="vs Baseline" Style="{StaticResource StatLabelStyle}"/>
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock x:Name="MemoryPerTabText" Text="5.6 KB" Style="{StaticResource StatValueStyle}"/>
                    <TextBlock Text="Memory per Tab" Style="{StaticResource StatLabelStyle}"/>
                </StackPanel>
            </Border>
            
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock x:Name="LeakCountText" Text="0" Style="{StaticResource StatValueStyle}"/>
                    <TextBlock Text="Potential Leaks" Style="{StaticResource StatLabelStyle}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Main Content Area -->
        <TabControl Grid.Row="2" x:Name="MainTabView">
            <!-- Service Breakdown Tab -->
            <TabItem Header="Service Breakdown">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="Top Memory Consumers" Style="{StaticResource SectionHeaderStyle}"/>
                        <DataGrid x:Name="ServiceBreakdownGrid" AutoGenerateColumns="False" 
                                  IsReadOnly="True" GridLinesVisibility="None"
                                  HeadersVisibility="Column" CanUserSortColumns="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Service" Binding="{Binding ServiceName}" Width="200"/>
                                <DataGridTextColumn Header="Total (KB)" Binding="{Binding TotalMemoryDeltaKB, StringFormat=F1}" Width="100"/>
                                <DataGridTextColumn Header="Avg/Op (KB)" Binding="{Binding AverageMemoryPerOperationKB, StringFormat=F1}" Width="100"/>
                                <DataGridTextColumn Header="Operations" Binding="{Binding OperationCount}" Width="100"/>
                                <DataGridTextColumn Header="Last Activity" Binding="{Binding LastOperationTime, StringFormat=HH:mm:ss}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Memory Leaks Tab -->
            <TabItem Header="Potential Leaks">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="Leak Suspects" Style="{StaticResource SectionHeaderStyle}"/>
                        <DataGrid x:Name="LeakSuspectsGrid" AutoGenerateColumns="False" 
                                  IsReadOnly="True" GridLinesVisibility="None"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Service" Binding="{Binding ServiceName}" Width="200"/>
                                <DataGridTextColumn Header="Est. Leak (KB)" Binding="{Binding TotalLeakEstimateKB, StringFormat=F1}" Width="120"/>
                                <DataGridTextColumn Header="Avg Leak/Op (KB)" Binding="{Binding AverageLeakPerOperationKB, StringFormat=F1}" Width="130"/>
                                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        
                        <TextBlock x:Name="NoLeaksText" Text="✅ No potential memory leaks detected" 
                                   HorizontalAlignment="Center" Margin="0,20"
                                   FontSize="14" Foreground="Green" Visibility="Collapsed"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Recent Activity Tab -->
            <TabItem Header="Recent Activity">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="Recent Operations" Style="{StaticResource SectionHeaderStyle}"/>
                        <DataGrid x:Name="RecentOperationsGrid" AutoGenerateColumns="False" 
                                  IsReadOnly="True" GridLinesVisibility="None"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Width="100"/>
                                <DataGridTextColumn Header="Operation" Binding="{Binding OperationName}" Width="200"/>
                                <DataGridTextColumn Header="Memory (KB)" Binding="{Binding MemoryDeltaKB, StringFormat=+0.0;-0.0;0.0}" Width="100"/>
                                <DataGridTextColumn Header="Duration (ms)" Binding="{Binding Duration.TotalMilliseconds, StringFormat=F1}" Width="110"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Memory Trends Tab -->
            <TabItem Header="Memory Trends">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="Memory Usage Trend" Style="{StaticResource SectionHeaderStyle}"/>
                        <Border Style="{StaticResource StatCardStyle}" HorizontalAlignment="Left">
                            <StackPanel>
                                <TextBlock x:Name="TrendStatusText" Text="Stable" FontSize="18" FontWeight="Bold"/>
                                <TextBlock x:Name="TrendDetailsText" Text="Change: 0.2 MB/min • Data points: 24" 
                                           Margin="0,4,0,0" Opacity="0.7"/>
                            </StackPanel>
                        </Border>
                        
                        <TextBlock Text="Tracking Information" Style="{StaticResource SectionHeaderStyle}"/>
                        <Border Style="{StaticResource StatCardStyle}" HorizontalAlignment="Left">
                            <StackPanel>
                                <TextBlock x:Name="TrackingDurationText" Text="Duration: 5m 23s" FontSize="14"/>
                                <TextBlock x:Name="SnapshotCountText" Text="Snapshots: 62" FontSize="14" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="FooterText" Text="Last updated: 14:23:15" 
                       VerticalAlignment="Center" Opacity="0.7"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Export Report" Click="ExportButton_Click" Margin="0,0,8,0" Padding="12,4"/>
                <Button Content="Close" Click="CloseButton_Click" Padding="12,4"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
