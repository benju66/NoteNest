<Window x:Class="NoteNest.UI.Dialogs.ModernInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" 
        Height="180" 
        Width="450"
        MinWidth="350"
        MaxWidth="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FocusManager.FocusedElement="{Binding ElementName=InputTextBox}"
        Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
    
    <Window.Resources>
        <!-- Theme-aware button styles (now respects user's theme choice) -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AppAccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextOnAccentBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppAccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AppAccentLightBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AppAccentLightBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AppAccentDarkBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AppAccentDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AppSurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AppSurfaceHighlightBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AppBorderHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AppSurfacePressedBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AppAccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title/Prompt -->
        <TextBlock Grid.Row="0" 
                   Name="PromptLabel" 
                   Text="Enter value:" 
                   FontSize="14"
                   FontWeight="Medium"
                   Margin="0,0,0,16"
                   Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
        
        <!-- Input TextBox with modern styling -->
        <TextBox Grid.Row="1" 
                 Name="InputTextBox" 
                 Margin="0,0,0,8"
                 Padding="12,8"
                 FontSize="13"
                 KeyDown="InputTextBox_KeyDown"
                 TextChanged="InputTextBox_TextChanged"/>
        
        <!-- Real-time validation feedback -->
        <TextBlock Grid.Row="2" 
                   Name="ErrorLabel" 
                   Foreground="{DynamicResource SystemControlErrorTextForegroundBrush}"
                   FontSize="12"
                   Visibility="Collapsed" 
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"/>
        
        <!-- Success indicator (optional) -->
        <TextBlock Grid.Row="3" 
                   Name="SuccessLabel"
                   Foreground="{DynamicResource SystemControlSuccessTextForegroundBrush}"
                   FontSize="12"
                   Visibility="Collapsed"
                   Margin="0,0,0,12"
                   Text="✓ Valid name"/>
        
        <!-- Action buttons with explicit styling for visibility -->
        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="OK" 
                    Name="OkButton"
                    Click="OkButton_Click" 
                    IsDefault="True"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="4,0"/>
            <Button Content="Cancel" 
                    Click="CancelButton_Click" 
                    IsCancel="True"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="4,0"/>
        </StackPanel>
    </Grid>
</Window>
